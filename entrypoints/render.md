# ftd-render

Intent: validate an existing canonical run and render requested formats without source reads or Test Design reruns.

This command is an optional alias; ordinary language reaches the same intent.
