# ftd-mcp

Intent: build and validate a target preview from the canonical suite. Require explicit approval before external writes; never delete by default; export a deterministic fallback if transport is unavailable.

This command is an optional alias; ordinary language reaches the same intent.
